# Evernode cluster monitor

This app can monitor a HotPocket cluster created by evdevkit.

Usage:
```
npm i

# nodes.json file must contain cluster create output from evdevkit.
node monitor.js nodes.json
```